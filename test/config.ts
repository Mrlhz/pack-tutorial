import port from './port.js'

export default {
	path: [
		// `http://localhost:${port.erp_demo}`,
		// `http://localhost:${port.erp_demo1}`,
		// `http://localhost:${port.erp_demo2}`,
		// `http://localhost:${port.erp_demo3}`
	],
	versionMap: {
		'erp-hello-world': '1.0.0.x',
		'erp-first-init': '1.0.0.x'
	}
}
